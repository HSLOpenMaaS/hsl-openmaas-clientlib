{
    "name": "server-example",
    "description": "Example server for ordering an HSL MaaS ticket",
    "version": "0.0.1",
    "license": "MIT",
    "engines": {
        "node": ">=8.10.0"
    },
    "scripts": {
        "start": "node --experimental-modules index.mjs"
    },
    "dependencies": {
        "axios": "^0.18.0",
        "dotenv": "^5.0.1",
        "express": "^4.16.3"
    },
    "devDependencies": {
        "nodemon": "^1.17.3"
    }
}
